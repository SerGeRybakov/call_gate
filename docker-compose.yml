services:
  redis:
    image: redis:latest
    ports:
      - 6379:6379
    restart: 'no'
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
